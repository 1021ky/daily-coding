[project]
name = "2026-01-29"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.pytest.ini_options]
addopts = "-v"
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
